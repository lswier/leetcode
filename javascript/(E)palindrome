/**
 * @param {number} x
 * @return {boolean}
 */
var isPalindrome = function(x) {
    
    var y = x.toString();
    var l = y.length;
    for (let i = 0; i < l; i++){
        if (y[i] !== y[l-i-1]){
            return false;
        }        
    }
    return true;
};
